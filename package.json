{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn lerna run build --stream --npm-client yarn",
    "bump-version": "yarn lerna version --conventional-commits",
    "release": "yarn lerna publish --conventional-commits"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "version": "independent"
}
